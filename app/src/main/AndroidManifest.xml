<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.richdroid.physicsbasedanimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.TranslateSpringAnimationActivity"
            android:label="@string/translate_spring_animation" />
        <activity
            android:name=".ui.activity.TranslateFlingAnimationActivity"
            android:label="@string/translate_fling_animation" />
        <activity
            android:name=".ui.activity.TranslateAndRotateSpringAnimationActivity"
            android:label="@string/translate_rotate_spring_animation" />
        <activity
            android:name=".ui.activity.ChainedSpringAnimationActivity"
            android:label="@string/chained_spring_animation" />
    </application>

</manifest>